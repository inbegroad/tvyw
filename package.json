{
  "name": "root",
  "packageManager": "yarn@3.2.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "deploy": "turbo run deploy",
    "dev": "turbo run dev",
    "test": "turbo run test"
  },
  "dependencies": {
    "turbo": "^1.3.1"
  }
}
